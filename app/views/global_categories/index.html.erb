<section class="space-y-6 fade-in">
  <div class="rounded-3xl border border-sky-100 bg-gradient-to-r from-sky-50 via-white to-cyan-50 p-5 shadow-sm md:p-6">
    <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
      <div>
        <h1 class="text-2xl font-extrabold tracking-tight text-slate-800">Categorias globales</h1>
        <p class="mt-1 text-sm text-slate-500">Catalogo compartido para clasificar productos globales.</p>
      </div>
      <%= link_to global_products_path,
                  class: "inline-flex cursor-pointer items-center justify-center gap-2 rounded-2xl border border-slate-300 bg-white px-5 py-3 font-medium text-slate-700 shadow-sm transition-all hover:bg-slate-50" do %>
        <i data-lucide="arrow-left" class="h-4 w-4"></i>
        Volver a productos globales
      <% end %>
    </div>
  </div>

  <div class="grid gap-6 lg:grid-cols-[minmax(0,24rem)_minmax(0,1fr)]">
    <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
      <h2 class="text-sm font-bold uppercase tracking-wider text-slate-500">Crear categoria global</h2>
      <div class="mt-4">
        <%= render "form", global_category: @global_category, submit_label: "Guardar categoria" %>
      </div>
    </div>

    <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
      <div class="flex items-center justify-between">
        <h2 class="text-sm font-bold uppercase tracking-wider text-slate-500">Categorias registradas</h2>
        <span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700"><%= @global_categories.size %></span>
      </div>

      <% if @global_categories.any? %>
        <div class="mt-4 overflow-hidden rounded-xl border border-slate-200">
          <table class="min-w-full text-left text-sm text-slate-700">
            <thead class="bg-slate-50 text-xs font-bold uppercase tracking-wider text-slate-500">
              <tr>
                <th class="px-4 py-3">Nombre</th>
                <th class="px-4 py-3 text-right">Productos globales</th>
                <th class="px-4 py-3 text-right">Acciones</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-slate-100">
              <% @global_categories.each do |global_category| %>
                <% products_count = @global_product_counts_by_category_name[global_category.name.to_s.strip].to_i %>
                <tr class="hover:bg-slate-50/80">
                  <td class="px-4 py-3 font-semibold text-slate-800"><%= global_category.name %></td>
                  <td class="px-4 py-3 text-right">
                    <span class="inline-flex items-center rounded-full bg-cyan-100 px-2.5 py-1 text-xs font-bold text-cyan-700"><%= products_count %></span>
                  </td>
                  <td class="px-4 py-3">
                    <div class="flex justify-end gap-2">
                      <%= link_to edit_global_category_path(global_category),
                                  class: "inline-flex cursor-pointer items-center justify-center rounded-lg bg-sky-50 p-2 text-sky-600 transition-colors hover:bg-sky-100",
                                  title: "Editar",
                                  aria: { label: "Editar categoria global" } do %>
                        <i data-lucide="pencil" class="h-4 w-4"></i>
                      <% end %>
                      <%= button_to global_category_path(global_category),
                                    method: :delete,
                                    class: "inline-flex cursor-pointer items-center justify-center rounded-lg bg-rose-50 p-2 text-rose-600 transition-colors hover:bg-rose-100",
                                    form: { data: { turbo_confirm: "Esta categoria se eliminara solo si no tiene productos globales asociados. Continuar?" } },
                                    title: "Eliminar",
                                    aria: { label: "Eliminar categoria global" } do %>
                        <i data-lucide="trash-2" class="h-4 w-4"></i>
                      <% end %>
                    </div>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="mt-4 rounded-xl border border-dashed border-slate-300 bg-slate-50 p-6 text-center">
          <p class="text-sm text-slate-600">Aun no hay categorias globales. Crea la primera para usarla en productos globales.</p>
        </div>
      <% end %>
    </div>
  </div>
</section>
