<style>
  .services-scope {
    --services-ink: #0f172a;
    --services-muted: #64748b;
    --services-accent: #0ea5e9;
    --services-sun: #f59e0b;
    --services-leaf: #22c55e;
  }

  .services-scope a {
    cursor: pointer;
  }

  .services-scope button,
  .services-modal button,
  .services-modal-content button {
    cursor: pointer;
  }

  .services-scope input[type="submit"] {
    cursor: pointer;
  }

  .services-scope button:disabled,
  .services-modal button:disabled,
  .services-modal-content button:disabled {
    cursor: not-allowed;
  }

  .services-scope input[type="submit"]:disabled {
    cursor: not-allowed;
  }

  .services-kicker {
    font-size: 0.7rem;
    letter-spacing: 0.35em;
    text-transform: uppercase;
    color: var(--services-accent);
    font-weight: 600;
  }

  .services-hero {
    background: linear-gradient(
      120deg,
      rgba(14, 165, 233, 0.08),
      rgba(34, 197, 94, 0.06),
      rgba(251, 146, 60, 0.1)
    );
  }

  .services-hero-glow {
    position: absolute;
    border-radius: 9999px;
    filter: blur(46px);
    opacity: 0.5;
    pointer-events: none;
  }

  .services-hero-glow--one {
    top: -80px;
    right: -20px;
    width: 220px;
    height: 220px;
    background: rgba(14, 165, 233, 0.28);
  }

  .services-hero-glow--two {
    bottom: -90px;
    left: -40px;
    width: 240px;
    height: 240px;
    background: rgba(34, 197, 94, 0.22);
  }

  .services-rate-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.85rem 1rem;
    border-radius: 1rem;
    border: 1px solid #e2e8f0;
    background: #fff;
    box-shadow: 0 8px 24px rgba(15, 23, 42, 0.05);
  }

  .services-rate-label {
    font-size: 0.7rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: #94a3b8;
    font-weight: 600;
  }

  .services-rate-value {
    font-size: 1rem;
    font-weight: 700;
    color: #0f172a;
  }

  .services-rate-pill {
    font-size: 0.65rem;
    font-weight: 600;
    padding: 0.25rem 0.6rem;
    border-radius: 9999px;
    background: rgba(14, 165, 233, 0.12);
    color: #0369a1;
  }

  .services-search {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    align-items: center;
  }

  .services-search-input {
    width: 100%;
    border-radius: 1rem;
    border: 1px solid #e2e8f0;
    padding: 0.75rem 2.5rem 0.75rem 2.5rem;
    font-size: 0.9rem;
    color: #0f172a;
    background: #fff;
    box-shadow: 0 8px 20px rgba(15, 23, 42, 0.04);
  }

  .services-search-input:focus {
    outline: none;
    border-color: rgba(14, 165, 233, 0.6);
    box-shadow: 0 0 0 3px rgba(14, 165, 233, 0.15);
  }

  .services-search-icon {
    position: absolute;
    left: 0.85rem;
    top: 50%;
    transform: translateY(-50%);
    color: #94a3b8;
  }

  .services-clear-btn {
    position: absolute;
    right: 0.75rem;
    top: 50%;
    transform: translateY(-50%);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 9999px;
    background: #f1f5f9;
    color: #64748b;
    transition: background 0.2s ease, color 0.2s ease;
  }

  .services-clear-btn:hover {
    background: #e2e8f0;
    color: #0f172a;
  }

  .services-secondary-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.65rem 1.4rem;
    border-radius: 9999px;
    border: 1px solid #e2e8f0;
    background: #fff;
    color: #0f172a;
    font-size: 0.85rem;
    font-weight: 600;
    box-shadow: 0 6px 18px rgba(15, 23, 42, 0.06);
    transition: transform 0.2s ease, border 0.2s ease;
  }

  .services-secondary-btn:hover {
    border-color: rgba(14, 165, 233, 0.4);
    transform: translateY(-1px);
  }

  .services-card {
    background: linear-gradient(160deg, #ffffff 0%, #f8fafc 100%);
    border: 1px solid #e2e8f0;
    transition: transform 0.25s ease, box-shadow 0.25s ease;
    animation: services-rise 0.82s ease both;
  }

  .services-frame-flow {
    display: contents;
  }

  html[data-turbo-preview] .services-card,
  body[data-turbo-preview] .services-card {
    animation: none !important;
  }

  .services-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 18px 40px rgba(15, 23, 42, 0.12);
  }

  .services-pill {
    font-size: 0.65rem;
    font-weight: 600;
    padding: 0.25rem 0.6rem;
    border-radius: 9999px;
    background: rgba(15, 23, 42, 0.75);
    color: #f8fafc;
  }

  .services-status {
    font-size: 0.65rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    padding: 0.25rem 0.6rem;
    border-radius: 9999px;
  }

  .services-status--ok {
    background: rgba(34, 197, 94, 0.15);
    color: #15803d;
  }

  .services-status--off {
    background: rgba(248, 113, 113, 0.15);
    color: #b91c1c;
  }

  .services-icon-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.2rem;
    height: 2.2rem;
    border-radius: 0.8rem;
    border: 1px solid #e2e8f0;
    background: #fff;
    color: #0f172a;
    transition: transform 0.2s ease, border 0.2s ease, box-shadow 0.2s ease;
  }

  .services-icon-btn:hover {
    transform: translateY(-1px);
    border-color: rgba(14, 165, 233, 0.4);
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.08);
  }

  .services-icon-btn--accent {
    background: rgba(14, 165, 233, 0.12);
    color: #0369a1;
    border-color: rgba(14, 165, 233, 0.25);
  }

  .services-icon-btn--danger {
    background: rgba(248, 113, 113, 0.12);
    color: #b91c1c;
    border-color: rgba(248, 113, 113, 0.25);
  }

  .services-modal {
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s ease;
  }

  .services-modal.is-open {
    opacity: 1;
    pointer-events: auto;
  }

  .services-modal-backdrop {
    background: radial-gradient(
      circle at top,
      rgba(15, 23, 42, 0.65),
      rgba(15, 23, 42, 0.85)
    );
  }

  .services-modal-panel {
    transform: translateY(18px) scale(0.98);
    opacity: 0;
    transition: transform 0.25s ease, opacity 0.25s ease;
  }

  .services-modal.is-open .services-modal-panel {
    transform: translateY(0) scale(1);
    opacity: 1;
  }

  .services-modal-close {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 9999px;
    border: 1px solid #e2e8f0;
    background: #fff;
    color: #64748b;
    transition: color 0.2s ease, border 0.2s ease;
  }

  .services-modal-close:hover {
    color: #0f172a;
    border-color: rgba(14, 165, 233, 0.5);
  }

  .services-price-card {
    border-radius: 1.5rem;
    border: 1px solid #e2e8f0;
    padding: 1.25rem;
    background: linear-gradient(
      145deg,
      rgba(14, 165, 233, 0.08),
      rgba(248, 250, 252, 0.9)
    );
  }

  .services-copy-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 1.1rem;
    border-radius: 9999px;
    border: 1px solid rgba(14, 165, 233, 0.3);
    background: rgba(14, 165, 233, 0.08);
    color: #0369a1;
    font-size: 0.8rem;
    font-weight: 600;
    transition: transform 0.2s ease, border 0.2s ease;
  }

  .services-copy-btn:hover {
    transform: translateY(-1px);
    border-color: rgba(14, 165, 233, 0.6);
  }

  .services-section-card {
    border-radius: 1.25rem;
    border: 1px solid #e2e8f0;
    padding: 1rem;
    background: #fff;
    box-shadow: 0 10px 24px rgba(15, 23, 42, 0.05);
  }

  .services-section-title {
    font-size: 0.75rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    font-weight: 700;
    color: #64748b;
  }

  .services-section-body {
    margin-top: 0.6rem;
    font-size: 0.9rem;
    color: #475569;
  }

  .services-section--warning {
    background: linear-gradient(
      160deg,
      rgba(251, 191, 36, 0.12),
      rgba(255, 255, 255, 0.9)
    );
    border-color: rgba(251, 191, 36, 0.4);
  }

  .services-section--info {
    background: linear-gradient(
      160deg,
      rgba(14, 165, 233, 0.12),
      rgba(255, 255, 255, 0.9)
    );
    border-color: rgba(14, 165, 233, 0.35);
  }

  .services-section--success {
    background: linear-gradient(
      160deg,
      rgba(34, 197, 94, 0.12),
      rgba(255, 255, 255, 0.9)
    );
    border-color: rgba(34, 197, 94, 0.35);
  }

  .services-section--danger {
    background: linear-gradient(
      160deg,
      rgba(248, 113, 113, 0.15),
      rgba(255, 255, 255, 0.9)
    );
    border-color: rgba(248, 113, 113, 0.35);
  }

  .services-section--neutral {
    background: linear-gradient(
      160deg,
      rgba(148, 163, 184, 0.12),
      rgba(255, 255, 255, 0.9)
    );
    border-color: rgba(148, 163, 184, 0.4);
  }

  .custom-copy-link {
    color: #0ea5e9;
    text-decoration: underline;
    font-weight: 600;
    word-break: break-all;
  }

  .copied-tooltip {
    position: absolute;
    left: 50%;
    top: -1.6em;
    transform: translateX(-50%);
    background: #0f172a;
    color: #fff;
    padding: 2px 10px;
    border-radius: 9999px;
    font-size: 0.7rem;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
  }

  .copied-tooltip.show {
    opacity: 0.95;
  }

  input[data-disable-number-steppers="true"]::-webkit-outer-spin-button,
  input[data-disable-number-steppers="true"]::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
  }

  input[data-disable-number-steppers="true"] {
    -moz-appearance: textfield;
    appearance: textfield;
  }

  @keyframes services-rise {
    from {
      opacity: 0;
      transform: translateY(16px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
