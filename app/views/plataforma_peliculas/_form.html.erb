<%= form_with(model: plataforma_pelicula) do |form| %>
  <% if plataforma_pelicula.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(plataforma_pelicula.errors.count, "error") %> prohibited this plataforma_pelicula from being saved:</h2>
      <ul>
        <% plataforma_pelicula.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= form.label :escala %>
    <%= form.number_field :escala, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
