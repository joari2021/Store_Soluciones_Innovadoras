<% if @header_notifications_count.to_i.positive? %>
  <span class="absolute -right-1 -top-1 inline-flex min-w-[1.1rem] items-center justify-center rounded-full bg-red-500 px-1.5 py-0.5 text-[10px] font-bold text-white ring-2 ring-white"><%= @header_notifications_count %></span>
<% end %>